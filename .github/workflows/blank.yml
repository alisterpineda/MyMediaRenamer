name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Build GUI project
      run: dotnet publish ${GITHUB_WORKSPACE}/MyMediaRenamer.Gui
